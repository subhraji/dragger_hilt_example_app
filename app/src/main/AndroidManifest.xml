<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HiltApp"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".broad_cast_reciever.TestBroadCastReceiverActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".coroutine_flow.LearnFlowActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="image/*" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activity.DataBindingTestActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true" />

        <!--//static broadcast receiver register
        <receiver
            android:name=".broad_cast_reciever.ExampleBroadCastReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>-->
        <receiver
            android:name=".broad_cast_reciever.OrderedReceiver2"
            android:exported="true">
            <intent-filter
                android:priority="2">
                <action android:name="com.example.test" />
            </intent-filter>
        </receiver>
        <receiver
            android:name=".broad_cast_reciever.OrderedReceiver3"
            android:exported="true">
            <intent-filter
                android:priority="3">
                <action android:name="com.example.test" />
            </intent-filter>
        </receiver>
    </application>

</manifest>